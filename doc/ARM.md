# ARM64 notes

The package uses Bitcoin Core's native `aarch64-linux-gnu` release archive.
ARM64 is supported from the first package version. Use reliable SSD/NVMe or
equivalent storage where possible and avoid putting blockchain data on an SD
card for sustained operation.

